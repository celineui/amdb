<p id="notice"><%= notice %></p>

<p><%= link_to "Movies by this director", director_movies_url(@director) %></p>
<p>
  <b>Name:</b>
  <%= @director.name %>
</p>

<p>
  <b>Dob:</b>
  <%= @director.dob %>
</p>

<p>
  <b>Filmography:</b>
  <ul>
    <% @director.movies.each do |movie| %>
      <li><%= link_to movie.title, director_movie_url(@director, movie) %></li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_director_path(@director) %> |
<%= link_to 'Back', directors_path %>
